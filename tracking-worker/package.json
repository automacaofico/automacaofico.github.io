{
  "name": "fico-tracking-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run --outdir dist",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.120.1"
  }
}
